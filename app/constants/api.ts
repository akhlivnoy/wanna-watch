// TODO: move URL to environment
// TODO: we can use react-native-env for this purpose
export const API_BASE_URL = '';

// 10s timeout to avoid really long calls
export const API_TIMEOUT = 10000;

// Activated by the default
export const API_DEFAULT_HEADERS = { 'Cache-Control': 'no-cache' };
